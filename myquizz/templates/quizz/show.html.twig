{% extends 'base.html.twig' %}

{% block title %}Quizz{% endblock %}

{% block body %}
    <h1 class="text-center m-2">Quizz</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quizz.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ quizz.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quizz_index') }}" class="btn btn-secondary m-1"><i class="fas fa-arrow-left mr-1"></i>Back</a>

    <a href="{{ path('quizz_edit', {'id': quizz.id}) }}" class="btn btn-primary m-1"><i class="far fa-edit"></i></a>

    {{ include('quizz/_delete_form.html.twig') }}
{% endblock %}
