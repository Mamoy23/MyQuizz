{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1 class="text-center m-1">Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}" class="btn btn-secondary m-1"><i class="fas fa-arrow-left mr-1"></i>Back</a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}" class="btn btn-primary m-1"><i class="far fa-edit"></i></a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
